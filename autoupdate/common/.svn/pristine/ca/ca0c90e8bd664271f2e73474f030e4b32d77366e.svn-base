#pragma once

#include <string>
using namespace std;

string GBKToUTF8(const char* strGBK);
string UTF8ToGBK(const char* strUTF);

bool UrlEncode(const char* szSrc, char* pBuf, int cbBufLen, bool bUpperCase,int flag = 1);